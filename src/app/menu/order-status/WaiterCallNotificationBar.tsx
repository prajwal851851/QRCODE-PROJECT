"use client";

import WaiterCallNotification from "../components/WaiterCallNotification";

export default function WaiterCallNotificationBar() {
  // This component simply renders the notification bar for reusability
  return <WaiterCallNotification showResolve={false} />;
}
